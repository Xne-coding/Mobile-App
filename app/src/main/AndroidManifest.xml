<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuestLearn">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.QuestLearn.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".OnboardingActivity"
            android:theme="@style/Theme.QuestLearn" />

        <activity android:name=".LoginActivity"
            android:theme="@style/Theme.QuestLearn" />

        <activity android:name=".SsoLoginActivity"
            android:theme="@style/Theme.QuestLearn" />

        <activity android:name=".MainActivity"
            android:theme="@style/Theme.QuestLearn" />

        <activity android:name=".ScanActivity"
            android:theme="@style/Theme.QuestLearn.Dark" />

        <activity android:name=".ChallengeDetailActivity"
            android:theme="@style/Theme.QuestLearn" />

    </application>

</manifest>
